ema_velocity_smoother_node:
  ros__parameters:
    input_topic: "cmd_vel"
    output_topic: "cmd_vel_smoothed"
    robot_base_frame: "vehicle"
    # Closer to 1.0 = highly responsive / jerky. Closer to 0.0 = very smooth / delayed.
    alpha_linear: 0.15
    alpha_angular: 0.25